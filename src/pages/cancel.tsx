import React from 'react'
import Layout from '../components/Layout'

const Cancel = () => {
    return (
        <Layout>
            <h1>Much Cancel!</h1>
        </Layout>
    )
}

export default Cancel
